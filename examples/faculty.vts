class faculty {
    action calc (base) {
        control.if(op.equal(base, 1)) {
            control.return(base);
        }
        control.return(op.mult(base, this.calc(op.sub(base, 1))));
    }

    action new () {

    }
}
