class fibonacci {
    action calc (base) {
        if(base == 1 OR base == 2) {
            return 1;
        }

        return this.calc(base - 1) + this.calc(base - 2);
    }
    
    action new () {

    }
}
