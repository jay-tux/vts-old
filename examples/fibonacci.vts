class fibonacci {
    action calc (base) {
        control.if(op.or(op.equal(base, 1), op.equal(base == 2))) {
            control.return(base);
        }

        control.return(op.add(this.calc(op.sub(base - 1)), this.calc(op.sub(base, 2))));
    }
    
    action new () {

    }
}
