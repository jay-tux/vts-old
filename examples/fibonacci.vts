class fibonacci {
    action calc is int(base is int) {
        if(base == 1 OR base == 2) {
            return base;
        }

        return fibonacci(base - 1) + fibonacci(base - 2);
    }
    
    action new is fibonacci() {

    }
}