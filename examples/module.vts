#load <faculty.vts>;
#load <fibonacci.vts>;

entry () {
    core.stdout(" --- Trying to calculate the faculty of the 6th Fibonacci number --- ");
    core.stdout(faculty.new().calc(fibonacci.new().calc(6)));
    core.stderr("No errors!");
}
